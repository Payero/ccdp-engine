<project name="Mesos Framework" default="main" basedir=".">
	<description>
		Create a Java Project (JAR) with Ant build script
	</description>

	<property name="project.name" value="mesos-dra" />
	<property name="executor.name" value="mesos-ccdp-exec" />
  <property name="engine.name" value="mesos-engine" />

	<!-- Java sources -->
	<property name="src.dir" location="src" />

	<!-- Java classes -->
	<property name="build.dir" location="build" />

	<!-- Output, Jar -->
	<property name="dist.dir" location="dist" />

	<!--Thirdparty jars/libaries -->
	<property name="lib.dir" location="lib" />

	<path id="classpath">
        <fileset dir="${lib.dir}" includes="**/*.jar"/>
    </path>


	<target name="init">
		<!-- Create the time stamp -->
		<tstamp />
		<!-- Create the build directory structure used by compile -->
		<mkdir dir="${build.dir}" />
	</target>

	<target name="compile" depends="init" description="compile the source ">
		<!-- Compile the java code from ${src.dir} into ${build.dir} -->
		<javac includeantruntime="false" 
				 debug="true"
			   srcdir="${src.dir}" 
			   destdir="${build.dir}" 
			   classpathref="classpath"/>
	</target>

	<target name="dist" depends="compile" description="package, output to JAR">

		<!-- Create the distribution directory -->
		<mkdir dir="${dist.dir}" />

		<!-- Put everything in ${build} into the {$projectName}-${DSTAMP}.jar file -->
		<jar jarfile="${dist.dir}/${project.name}-${DSTAMP}.jar" basedir="${build.dir}" >
		    <manifest>
			<!-- create an executable Jar -->
			<attribute name="Main-Class" value="com.axios.ccdp.mesos.fmwk.CcdpCommandExecutor" />
		    </manifest>
		</jar>
	</target>

	<target name="clean" description="clean up">
		<delete dir="${build.dir}" />
		<delete dir="${dist.dir}" />
	</target>
	
	<target name="gen.jars" depends="compile">
		<copy file="${basedir}/config/log4j.properties" todir="${build.dir}" />
    <jar destfile="${dist.dir}/${executor.name}.jar" >
        <manifest>
            <attribute name="Main-Class" value="com.axios.ccdp.mesos.fmwk.CcdpCommandExecutor"/>
        </manifest>
        <fileset dir="${build.dir}" includes="*.properties" />
  		<fileset dir="${build.dir}"/>

        <zipgroupfileset dir="${lib.dir}" includes="*.jar"/>
    </jar>

    <copy file="${basedir}/config/ccdp-config.properties" todir="${build.dir}" />
    <jar destfile="${dist.dir}/${engine.name}.jar" >
        <manifest>
            <attribute name="Main-Class" 
                       value="com.axios.ccdp.mesos.fmwk.CCDPEngineMain"/>
        </manifest>
        <fileset dir="${build.dir}" includes="*.properties" />
      <fileset dir="${build.dir}"/>

        <zipgroupfileset dir="${lib.dir}" includes="*.jar"/>
    </jar>

	</target>

	<!-- Default, run this -->
	<target name="main" depends="clean, compile, dist, gen.jars" />

</project>