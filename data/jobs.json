{
  "jobs": 
    [
      
      {"cpus": 0.5, "mem": 100, "command": "docker run ubuntu /bin/echo 'Hello world' >> /tmp/docker.txt"},
      {"cpus": 0.5, "mem": 100, "command": "/bin/echo 'Hello world' >> /tmp/hello.txt"},

    ]
}
